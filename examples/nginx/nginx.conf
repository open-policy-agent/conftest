worker_processes 1;

http {
    server {
        listen 80;
        server_name example.com;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name example.com;

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
